LOCAL_PATH := $(call my-dir)

include $(CLEAR_VARS)

LOCAL_PACKAGE_NAME := BIST
LOCAL_MODULE_TAGS := optional

LOCAL_SRC_FILES := \
	$(call all-java-files-under, src) \
	$(call all-subdir-Iaidl-files)
#	$(call all-named-files-under,*.aidl,.) # all-subdir-Iaid-files only includes I*.aidl; see definitions.mk

LOCAL_AIDL_INCLUDES := $(LOCAL_PATH)/aidl
LOCAL_STATIC_JAVA_LIBRARIES := \
	androidx.appcompat_appcompat \
	androidx.startup_startup-runtime \
    androidx_media3_media3-exoplayer_dependencies \
    androidx.media3_media3-ui_dependencies

LOCAL_CERTIFICATE := platform
LOCAL_DEX_PREOPT := false
#LOCAL_SDK_VERSION := system_current
#LOCAL_MIN_SDK_VERSION := 26

LOCAL_PROGUARD_ENABLED := disabled
#LOCAL_PROPRIETARY_MODULE := true
LOCAL_SYSTEM_EXT_MODULE := true
LOCAL_PRIVILEGED_MODULE := true
LOCAL_PRIVATE_PLATFORM_APIS := true

include $(BUILD_PACKAGE)
